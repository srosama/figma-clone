/*
Dashboard:
    Save the designFile (utiltes.generatorHelper.tsx) -> save it in the context 
    redirect to (editor/:id) 'the editor will consime the data from the id from the context' 
    ! create a list in the contetx and render it (DesingFileObject.tsx)

Editor:
    editor/:id/update ->
    editor/:id/delete ->

liveblock: 
*/